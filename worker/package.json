{
  "name": "alsultan-rfq",
  "version": "1.0.0",
  "private": true,
  "description": "RFQ intake + quote builder backend for Alsultan Dates",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute alsultan --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute alsultan --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
